name: check
on:
  pull_request:
    branches:
      - main
jobs:
  check:
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - uses: Pistonight/mono-dev/actions/setup@main
        with:
          rust: stable
          tool-cargo-binstall: layered-crate
      - run: task check
